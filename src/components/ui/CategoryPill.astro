---
interface Props {
  category: string;
}

const { category } = Astro.props;
---

<span class="px-3 py-1 text-sm rounded-full bg-primary/80 text-white border border-primary shadow-md flex items-center justify-center leading-none font-semibold backdrop-blur-sm">
  {category}
</span> 